<form action="/favourites" method="POST" class="inline-form">
  <input type="hidden" name="id" value="<%= home._id %>" />
  <button type="submit" class="btn btn-soft">
    <i class="fa-regular fa-heart"></i> Save
  </button>
</form>
